{
  "name": "@use-gpu/plot",
  "version": "0.5.0",
  "main": "src/index.ts",
  "author": "Steven Wittens <steven@acko.net>",
  "license": "MIT",
  "private": true,
  "dependencies": {
    "@use-gpu/workbench": "*",
    "@use-gpu/core": "*",
    "@use-gpu/live": "*",
    "@use-gpu/shader": "*",
    "mikktspace": "^1.1.1"
  },
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "build": "NPM_PACKAGE=plot ../../scripts/build-mjs-umd.sh",
    "test": "jest --watch",
    "lint": "eslint src --ext ts",
    "coverage": "echo 'jest --collect-coverage' disabled",
    "types": "tsc --noEmit",
    "types:watch": "yarn run types -- --watch"
  }
}