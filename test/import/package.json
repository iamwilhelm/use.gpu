{
  "name": "@use-gpu/shader",
  "version": "0.0.1",
  "author": "Steven Wittens <steven@acko.net>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "test": "rm -rf ./dist; rm -rf ./node_modules; mkdir ./node_modules 2>/dev/null; mkdir ./node_modules/@use-gpu 2>/dev/null; cp -r ../../build/packages/* node_modules/@use-gpu; yarn run test:ts-node && yarn run test:webpack",
    "test:ts-node": "../../node_modules/.bin/ts-node ./src/ts-node.ts",
    "test:webpack": "../../node_modules/.bin/webpack build; ../../node_modules/.bin/ts-node ./src/webpack.ts"
  }
}
